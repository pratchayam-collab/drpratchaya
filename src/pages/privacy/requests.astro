---
export const prerender = false;
import BaseLayout from '~/layouts/BaseLayout.astro';
import DataSubjectRequestForm from '~/components/legal/DataSubjectRequestForm.astro';
import type { Locale } from '~/config/site.config';
import { localePath } from '~/i18n/ui';

const locale = 'th' satisfies Locale;
---

<BaseLayout
  locale={locale}
  title="คำขอข้อมูลส่วนบุคคล"
  description="ขอสำเนาหรือลบข้อมูลการจองที่ผูกกับอีเมลของคุณ"
>
  <div class="page">
    <header class="legal-head">
      <h1 class="legal-title">คำขอข้อมูลส่วนบุคคล</h1>
      <p class="legal-intro measure">
        ระบบจะส่งรหัสยืนยันไปยังอีเมลที่คุณระบุก่อนดำเนินการ อ่านรายละเอียดใน{' '}
        <a href={localePath(locale, 'privacy/')}>นโยบายความเป็นส่วนตัว</a>
      </p>
    </header>
    <DataSubjectRequestForm locale={locale} />
  </div>
</BaseLayout>

<style>
  .legal-head {
    padding-top: 2.5rem;
    padding-bottom: 1.5rem;
  }

  .legal-title {
    font-size: var(--text-title);
    line-height: 1.3;
  }

  .legal-intro {
    margin-top: 0.75rem;
    font-size: var(--text-lead);
    line-height: 1.75;
    color: var(--text-2);
  }

  .legal-intro a {
    color: var(--teal-deep);
    font-weight: 600;
  }
</style>
